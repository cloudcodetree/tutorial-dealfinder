.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.ruff_cache/
dealfinder.sqlite

.env

# Local `uv` resolution artifact. The course installs with `pip install -e ".[dev]"`
# (README, Dockerfiles, CI) — nothing here uses uv, so a committed lock would only rot.
uv.lock
